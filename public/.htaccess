<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Never serve index.html for a removed hashed asset. Returning HTML for a
  # JavaScript request causes a MIME error and leaves the SPA as a white page.
  RewriteCond %{REQUEST_URI} ^/assets/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^ - [R=404,L]

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>

<IfModule mod_headers.c>
  Header set X-Powered-By "Hostinger Horizons"

  # The entry files use stable names. Revalidate HTML and assets on every load
  # so a deployment cannot combine files from two different versions.
  Header always set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
  Header always set Pragma "no-cache"
  Header always set Expires "0"
</IfModule>
